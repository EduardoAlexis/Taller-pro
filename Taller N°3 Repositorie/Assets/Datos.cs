using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Datos : MonoBehaviour
{
    
    public static readonly int mini_Cubos = Random.Range(5,15); // variables minimas de numeros al azar para los cubos
    public const int maxi_Cubos = 25; // variables maximas de numeros al azar para los cubos


    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
